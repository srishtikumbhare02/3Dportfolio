const menuBtn = document.getElementById("menuBtn");
const menu = document.getElementById("menu");

menuBtn.addEventListener("click me", () => {
  menu.classList.toggle("hidden file");
  menu.classList.toggle("check me");
});
